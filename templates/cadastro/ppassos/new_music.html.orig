{% extends "site_base.html" %}

{% load i18n %}
{% load account_tags %}
{% load uni_form_tags %}

{% block head_title %}{% trans "Primeiro passo" %}{% endblock %}

{% block body %}
<<<<<<< local
     {% if email_address %}
        <h1>{% trans "E-mail Confirmado" %}</h1>
        {% user_display email_address.user as user_display %}
        <p>{% blocktrans with email_address.email as email %}{{ user_display }}, você confirmou que {{ email }} é o seu email.{% endblocktrans %}</p>
        <p>Continue e siga três passos para completar seu cadastro</p>
        <div>
        <h1>Primeiro passo</h1>
        <form class="signup uniForm" id="signup_form" method="post" action="/dev/cadastro/musicas/">
            {% csrf_token %}
            <fieldset class="inlineLabels">
                {{ form|as_uni_form }}
                <div class="form_block">
                    <input type="hidden" name="user_id" value="{{ email_address.user.id }}" />
                    <input type="submit" value="Salvar"/>
                    <input type=button onClick="location.href='/dev/cadastro/fotos/'" value='Pular'>
                </div>
            </fieldset>
        </form>
        </div>
    {% else %}
        <p>{% trans "Chave de confirmação inválida" %}.</p>
        <p>{% blocktrans %}Se você tiver qualquer problema criando sua conta, entre em contato conosco <a href="mailto:{{ CONTACT_EMAIL }}">{{ CONTACT_EMAIL }}</a>.{% endblocktrans %}</p>
    {% endif %}
=======
    <h1>{% trans "Músicas" %}</h1>
    <p>Continue e siga três passos para completar seu cadastro</p>
    {{ mensagem_erro|safe }}
    <div>
    <h1>Primeiro passo</h1>
    <form class="signup uniForm" id="signup_form" method="post" action="/cadastro/musicas/">
        {% csrf_token %}
        <fieldset class="inlineLabels">
            {{ form|as_uni_form }}
            <div class="form_block">
                <input type="submit" value="Salvar"/>
                <input type=button onClick="location.href='/cadastro/fotos/'" value='Pular'>
            </div>
        </fieldset>
    </form>
    </div>
>>>>>>> other
{% endblock %}

